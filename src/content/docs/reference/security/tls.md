---
draft: true
title: "TLS"
description: "Securing MQTT and API connections."
products: ["standard", "enterprise-management"]
roles: ["administrator"]
introduced: "1.0.0"
contentType: "reference"
lastReviewed: "2026-07-30"
---

Use TLS for broker and API traffic where supported — MQTT commonly on port `8883`.

:::note[Confirm TLS options]
Document SiteSync's TLS options (custom CA upload, certificate verification toggles, mutual TLS). Confirm with SiteSync Product.
:::
